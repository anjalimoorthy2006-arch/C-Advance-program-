#include <stdio.h>

int main() {
    char str[100];
    int count = 0, i;

    printf("Enter a string: ");
    gets(str);   // reads the string

    for (i = 0; str[i] != '\0'; i++) {
        count++;
    }

    printf("Number of characters = %d\n", count);

    return 0;
}